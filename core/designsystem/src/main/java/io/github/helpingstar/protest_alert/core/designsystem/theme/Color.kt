package io.github.helpingstar.protest_alert.core.designsystem.theme

import androidx.compose.ui.graphics.Color

/**
 * Core color palette for the Protest Alert design system.
 * These colors are used throughout the app for consistent theming.
 */
object PaColor {
    // Text
    val textPrimary = Color(0xFF171A1F)
    val textSecondary = Color(0xFF565D6D)
    val textPlaceholder = Color(0xFF9095A0)
    val textDisabled = Color(0xFF9095A0)
    val textOnAccent = Color(0xFFF5F5F5)

    // Background
    val backgroundSurface = Color.White
    val backgroundDisabled = Color(0xFFDEE1E6)

    // Surface
    val surfaceCard = Color(0xFFF3F4F6)

    // Chip
    val chipSurfaceEnabled = Color(0xFFEBF5FF)
    val chipSurfaceDisabled = Color(0xFFF3F4F6)
    val chipTextDisabled = Color(0xFF374151)

    // Border
    val borderDefault = Color(0xFFDEE1E6)

    // Accent
    val accentPrimary = Color(0xFF3899FA)
}

